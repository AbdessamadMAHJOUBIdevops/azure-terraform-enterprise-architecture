
server {
    # On utilise une variable Jinja2 pour le port !
    listen {{ http_port }};
    listen [::]:{{ http_port }};

    root {{ app_dir }};
    index index.html index.htm;

    server_name _;

    location / {
        try_files $uri $uri/ =404;
    }
}